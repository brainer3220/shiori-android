<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/shiori_canvas">

    <LinearLayout
        android:id="@+id/webview_header"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="8dp">

        <ImageButton
            android:id="@+id/webview_close_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_close"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_webview_close" />

        <ImageButton
            android:id="@+id/webview_back_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_back"
            android:enabled="false"
            android:alpha="0.4"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_chevron_up" />

        <ImageButton
            android:id="@+id/webview_forward_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_forward"
            android:enabled="false"
            android:alpha="0.4"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_chevron_down" />

        <Space
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <ImageButton
            android:id="@+id/webview_archive_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_archive"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_archive" />

        <ImageButton
            android:id="@+id/webview_star_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_star"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_star" />

        <ImageButton
            android:id="@+id/webview_more_button"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_more"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_more_horizontal" />
    </LinearLayout>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="56dp">

        <WebView
            android:id="@+id/webview_content"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <TextView
            android:id="@+id/webview_notion_placeholder"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone"
            android:text="@string/webview_notion_placeholder"
            android:textColor="@color/shiori_text_muted" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/webview_ask_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginHorizontal="16dp"
        android:layout_marginBottom="20dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="20dp"
        android:paddingEnd="8dp"
        android:paddingVertical="6dp"
        android:background="@drawable/bg_ask_bar">

        <EditText
            android:id="@+id/webview_ask_input"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:background="@null"
            android:hint="@string/webview_ask_hint"
            android:importantForAutofill="no"
            android:inputType="text"
            android:maxLines="1"
            android:textColor="@color/shiori_text_primary"
            android:textColorHint="@color/shiori_text_muted" />

        <ImageButton
            android:id="@+id/webview_ask_send"
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/cd_webview_ask_send"
            app:tint="@color/shiori_text_secondary"
            app:srcCompat="@drawable/ic_arrow_up" />
    </LinearLayout>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
